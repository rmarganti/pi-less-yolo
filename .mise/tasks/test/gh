#!/usr/bin/env bash
#MISE description="Smoke test that gh is available in the container"
set -euo pipefail

DOCKER_CMD="${PI_CONTAINER_RUNTIME:-docker}"
"${DOCKER_CMD}" run --rm pi-less-yolo:latest gh --version
